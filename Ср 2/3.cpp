#include<iostream>
#include <stdio.h>
#include<conio.h>
#include <string>
using namespace std;
void random(int* arr, int size) {
    for (int i = 0; i < size; i++)
        arr[i] = 10 - rand() % 20;

}
int main(int argc, char* argv[])
{
    int SIZE = 0;
    int* arr;
    cout << "Enter array size: ";
    cin >> SIZE;
    arr = new int[SIZE];
    int i = 0, j = 0, cnt = 0, max_cnt = 0;
    int is_seq = 0; // ???? ??????????? ?????????????????? ????????
    random(arr, SIZE);
    cout << "Array: ";
    for (int i = 0; i < SIZE; i++)
        cout <<to_string(arr[i]) + " ";
    cout << endl;
    for (i = 0; i < SIZE; i++) {
        j = i + 1;
        if (arr[i] == arr[j] && i < (SIZE - 1)) {
            is_seq = 1; //?????????????????? ?????????? ??? ????????????
            cnt++;
        }
        else {
            if (is_seq == 1) {               //???? ?????????????????? ???????????
                if (cnt > max_cnt)
                    max_cnt = cnt;      //?????? ???????? ?? ???-?? ??????????
            }
            is_seq = 0;
            cnt = 0;
        }
    }

    cout<<"Max sequence length is : "<<max_cnt + 1;    // ????? ?????????????????? ????? ????? ?????????? ???? 1 (?????? ????????)
    _getch();
    return 0;
}